# Core web framework
streamlit>=1.28.0,<1.35.0

# Numerical and scientific computing
numpy>=1.22.0,<1.27.0  # Ensure compatibility with TF and others
scipy>=1.8.0,<1.13.0

# Audio processing
librosa>=0.9.0,<0.11.0
soundfile>=0.11.0,<0.13.0
noisereduce>=2.0.0,<3.1.0

# Deep Learning Backend for Spleeter (TensorFlow is common)
# Spleeter 2.4 often works well with TF 2.10
tensorflow>=2.10.0,<2.11.0
# OR if you know you need CPU-only tensorflow:
# tensorflow-cpu>=2.10.0,<2.11.0

# Stem Separation library
spleeter>=2.4.0,<2.5.0

# Other utilities (likely installed as dependencies, but good to list)
protobuf<4.0.0 # Often needed for TensorFlow compatibility issues
requests
packaging